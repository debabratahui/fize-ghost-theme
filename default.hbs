<!DOCTYPE html>
<html lang="{{@site.locale}}" >
<head>
    {{!-- Basic meta - advanced meta is output with {ghost_head} below --}}
    <title>{{meta_title}}</title>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />


    {{!-- Bootstrap CSS --}}
    <link rel="stylesheet" href="{{asset "css/bootstrap.min.css"}}">

    {{!-- Theme assets - use the {asset} helper to reference styles & scripts,
    this will take care of caching and cache-busting automatically --}}
    <link rel="stylesheet" type="text/css" href="{{asset "css/style.css"}}" />

    {{!-- This tag outputs all your advanced SEO meta, structured data, and other important settings,
    it should always be the last tag before the closing head tag --}}
    {{ ghost_head }}

</head>

<body class="{{body_class}}">

    {{!-- Navigation bar --}}
    <nav class="navbar navbar-expand-lg navbar-light navbar-space-evenly">
        {{!-- <a class="navbar-brand" href="#">{{@site.title}}</a> --}}
        <a class="navbar-brand" href="#"><img src="{{@site.logo}}" alt="logo"></a>
        <button class="btn btn-get-started" type="button" data-toggle="modal" aria-label="Get started" data-target="#exampleModal">
            {{!-- <span class="navbar-toggler-icon"></span> --}}
            Get Started
        </button>

        {{!-- <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                <li class="nav-item active">
                    <a class="nav-link" href="/">Home</a>
                </li>
            </ul>
        </div> --}}
    </nav>
    {{!-- End Navbar --}}

    {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
    {{{body}}}

    {{!-- Footer content goes here --}}
    {{!-- <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12"> --}}
                <div class="card">
                    <div class="card-footer text-muted">
                        <div class="container">
                            <div class="row">
                                <div class="col-md-6">
                                    
                                        &copy; {{@site.title}} {{date format="YYYY"}}. All right reserved
                                </div>
                                <div class="col-md-3">
                                    Privacy Policy
                                </div>
                                <div class="col-md-3">
                                    Terms of Service
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {{!-- </div>
        </div>
    </div> --}}
    {{!-- End of footer --}}

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <iframe src="https://demo.getfize.io/?host=rate-based" height="627px" width="386px"></iframe>
      </div>
    </div>
  </div>
</div>
    

    {{!-- Ghost outputs required functional scripts with this tag - it should always be the last thing before the closing body tag --}}
    {{ ghost_foot }}

    <!-- Optional JavaScript -->
    <!-- jQuery Slim first, then Popper.js, then Bootstrap JS -->
    <script src="{{asset "js/jquery-3.6.0.min.js"}}"></script>
    <script src="{{asset "js/popper.min.js"}}"></script>
    <script src="{{asset "js/bootstrap.min.js"}}"></script>
</body>
</html>